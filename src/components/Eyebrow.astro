---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span
  class:list={[
    'eyebrow',
    'inline-block text-xs font-semibold tracking-[0.22em] uppercase text-amber-700 mb-4',
    className,
  ]}
>
  <slot />
</span>
