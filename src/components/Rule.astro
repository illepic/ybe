---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class:list={['rule', 'block', 'w-13', 'h-0.5', 'bg-purple-400', 'mx-auto', 'my-6', className]}
></span>
