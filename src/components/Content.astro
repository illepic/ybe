---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div
  class:list={['content', 'max-w-2xl', 'mx-auto', 'text-shadow-lg', 'text-stone-400', className]}
>
  <slot />
</div>
